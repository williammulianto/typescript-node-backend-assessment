import { describe, it, expect } from 'vitest';

describe('ProjectService testing', () => {
  describe('', () => {
    it('2+2 should result in 4', async () => {
      const add = (a: number, b: number) => a + b;
      expect(add(2, 2)).toBe(4);
    });
  });
});
